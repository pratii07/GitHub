#include<stdio.h>
int main()
{
    int a;
    printf("enter the num");
    scanf("%d",&a);
    if(a>0)
    {
    printf("the num is %d is positive", a);
    }
    else if(a<0)
    {
    printf("the num is %d is negetive", a);
    }
    else
    {
    printf("the num is %d is zero", a);
    }
    }


