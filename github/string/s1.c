//taking input and output in string.
/*#include<stdio.h>     //only 1st word give.
#include<string.h>
int main()
{
    char str[40];
    fgets(str); //input
    puts(str);  //for output & also give \n.(newline vr input deto actomatically)
    return 0;
}

#include<stdio.h>     //only 1st word gives.
#include<string.h>
int main()
{
    char str[40];
    printf("Enter the character : ");
    scanf("%s",str);
    printf("output is: %s",str);
    return 0;
}
#include<stdio.h>      //whole sentence gives.
#include<string.h>
int main()
{
    char str[40];
    printf("Enter the character : ");
    scanf("%[^\n]s",str);   //use [^\n]
    printf("output is: %s",str);
    return 0;
}*/
